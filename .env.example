# Copy to .env for local development. Never commit a real .env.
# These are public, browser-safe values only. No service-role key belongs in
# committed configuration.
VITE_SUPABASE_URL="https://your-project.supabase.co"
VITE_SUPABASE_PUBLISHABLE_KEY="sb_publishable_xxxxxxxxxxxxxxxxxxxx"
VITE_SUPABASE_PROJECT_ID="your-project-ref"

# Server-side mirrors of the same public values (used during SSR)
SUPABASE_URL="https://your-project.supabase.co"
SUPABASE_PUBLISHABLE_KEY="sb_publishable_xxxxxxxxxxxxxxxxxxxx"
SUPABASE_PROJECT_ID="your-project-ref"

# Privileged server operations use the same Solaris Studio Supabase project.
# Configure its service-role / secret key only in the server runtime, never in Git.
SUPABASE_SERVICE_ROLE_KEY="your-local-server-only-secret"

# Televoting lives in the `televoting` schema of the Solaris Studio project and
# therefore needs no separate URL, project ID, publishable key, or service key.
