export function ConfirmationsAdminNav(_props: { current?: string }) {
  // Confirmations admin routes are now rendered inside the unified Solaris
  // Operations shell. Keeping the old horizontal nav here created two competing
  // navigation systems on every page, so this compatibility component is now a
  // deliberate no-op while existing route imports remain valid.
  return null;
}
