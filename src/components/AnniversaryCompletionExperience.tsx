export function AnniversaryCompletionExperience() {
  // Disabled until these additions are mounted route-natively. The old version
  // appended a portal host into .app-main, which could destabilize client-side
  // route transitions and should never be allowed to crash the underlying page.
  return null;
}
