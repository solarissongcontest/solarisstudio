export function AnniversaryTasteEra() {
  // Disabled until Taste DNA owns the anniversary block in its normal route
  // tree. Portal injection into live page DOM was unsafe during navigation.
  return null;
}
