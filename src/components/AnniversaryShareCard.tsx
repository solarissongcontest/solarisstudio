export function AnniversaryShareCard() {
  // Disabled until the personalized share card is mounted by MySolaris itself.
  // The old portal-based insertion could interfere with route DOM ownership.
  return null;
}
