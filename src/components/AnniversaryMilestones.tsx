export function AnniversaryMilestones() {
  // Disabled until milestone cards are rendered inside the owning route tree.
  // The former portal host mutated React-managed DOM during navigation.
  return null;
}
